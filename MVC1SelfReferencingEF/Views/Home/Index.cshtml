@model IEnumerable<MVC1SelfReferencingEF.Models.Employee>

@{
    ViewBag.Title = "Index";
}


@*@foreach (var item in Model)
{
    <ul>
        <li>@Html.DisplayFor(m => item.EmployeeName) Manager: @Html.DisplayFor(m => item.Manager.EmployeeName)</li>
    </ul>
}*@

@foreach (var item in Model)
    {
        <ul>
            @
        </ul>
    }

@*@{
    <ul>
        @foreach (var item in Model)
        {
            


        }
    </ul>
}*@